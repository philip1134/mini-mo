# Specify project dependencies in this file,
# and run the following command to install your bundle
#
#   $ pip install -r path/to/this/requirements.txt
#

# package-name==package-version
selenium==3.5.0