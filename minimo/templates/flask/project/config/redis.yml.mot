# Redis configuration.
#


development:
    password:
    host: "127.0.0.1"
    port: 6379
    database: 1

test:
    password: $${project_abbreviation}_REDIS_PASSWORD
    host: $${project_abbreviation}_REDIS_HOST
    port: $${project_abbreviation}_REDIS_PORT
    database: $${project_abbreviation}_REDIS_DATABASE

staging:
    password: $${project_abbreviation}_REDIS_PASSWORD
    host: $${project_abbreviation}_REDIS_HOST
    port: $${project_abbreviation}_REDIS_PORT
    database: $${project_abbreviation}_REDIS_DATABASE

production:
    password: $${project_abbreviation}_REDIS_PASSWORD
    host: $${project_abbreviation}_REDIS_HOST
    port: $${project_abbreviation}_REDIS_PORT
    database: $${project_abbreviation}_REDIS_DATABASE