# Project dependencies under production environment,
# and run the following command to install your bundle
#
#   $ pip install -r path/to/this/production.txt
#

-r base.txt

# package-name==package-version