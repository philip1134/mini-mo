# Project dependencies under staging environment,
# and run the following command to install your bundle
#
#   $ pip install -r path/to/this/staging.txt
#

-r base.txt

# package-name==package-version